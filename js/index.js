const numSquares = 6;
let colors = [];
const colorDisplay = document.getElementById('colorDisplay');

let generateRandomColors = (num) => {
    let arr = [];
    num.forEach(element => {
        arr.push(element)
    });
}

let randomColor = () => {
    
}